{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git diff --quiet HEAD 2>/dev/null && git diff --cached --quiet 2>/dev/null && exit 0; FILES=$(git diff --name-only HEAD 2>/dev/null | head -3 | tr '\\n' ' '); git add -A && git commit -m \"auto: checkpoint $(date +'%H:%M') – ${FILES}\" 2>/dev/null || true",
            "statusMessage": "Committing changes..."
          }
        ]
      }
    ]
  }
}
